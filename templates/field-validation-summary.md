# Intake Sequencing Field Validation

Record package version, exact commit, validator versions, synthetic
cardinalities, Legacy Adoption cardinalities, malformed fixtures, shell parity,
read-only hashes, preset composition, agent cardinality, release ZIP checksum,
fleet status, residual risk, and reevaluation trigger.

Record an explicit zero-target `Idle` fixture and negative cases for an idle
series with content and a non-idle series without targets. This proves that an
aggregator repository does not need a fabricated intake.
